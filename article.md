Today i will write about my experience learning Spring Boot.

It is my second project using Spring Boot, the first one was a simple CRUD API for an blog.

## how to run this application

First, you need to have docker installed on your machine.

```bash
docker-compose up -d
```

```bash
./mvnw spring-boot:run
```


Let's start!

First, config the database settings in the `application.properties` file.
## link the doc ##

